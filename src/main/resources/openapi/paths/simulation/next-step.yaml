post:
  operationId: next_step
  summary: Computes the next step in the simulation
  tags:
    - simulation
  responses:
    200:
      description: Successfully going to the next step
      content:
        application/json:
          schema:
            $ref: "../../models/simulation/building-state.yaml#/BuildingState"